export interface styles {
  width: string;
  height: string;
}

export interface center2 {
  lat: number;
  lng: number;
}

export interface option {
  styles: any;
  disableDefaultUI: boolean;
  zoomControl: boolean;
}
